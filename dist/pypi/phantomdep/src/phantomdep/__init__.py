"""PhantomDep — local-first dependency firewall for AI coding agents.

This Python package is a thin wrapper that ships the precompiled phantomdep
binary in `phantomdep/bin/`. The Python layer exists so `pip install
phantomdep` works for the Python audience without requiring Rust.

For the full docs see https://github.com/openintelligence-labs/phantomdep.
"""

__version__ = "1.0.1"
